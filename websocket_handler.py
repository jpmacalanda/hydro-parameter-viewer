import logging
import json
from typing import Set, Dict, Any, Optional, Tuple, List, Union

import http_handler
import serial_processor

logger = logging.getLogger("hydroponics_server")

# This file is kept as a placeholder to maintain compatibility with imports
# but its functionality has been removed as we're no longer using WebSockets
logger.info("WebSocket handler is deprecated and not being used")
